# 项目技术设计文档：Browser Kernel —— 浏览器中的Windows兼容层

> **项目代号**：Browser Kernel  
> **核心理念**：在浏览器中通过WASM + HLE（高级别模拟）技术，原生运行Windows x86应用程序，并提供物理USB设备访问能力（锦上添花特性）  
> **技术哲学**：不模拟硬件，只模拟API接口 —— 让Windows程序以为自己运行在真实的Windows环境中


## 第一部分：核心架构概览

### 整体分层架构（从底层到上层）

| 层级 | 名称 | 运行环境 | 核心职责 |
|------|------|----------|----------|
| L0 | 物理硬件层 | 宿主机 | 真实USB设备、音视频硬件、GPU |
| L1 | 浏览器宿主层 | 浏览器 | WebUSB API、OPFS、WebGPU、AudioWorklet、Web Workers |
| L2 | 系统调用桥接层 | TypeScript/JS | WebUSB→WASI-USB适配器、OPFS文件适配器、图形命令转发 |
| L3 | Windows兼容核心层 | WASM | x86指令JIT翻译器、Windows API拦截器、进程/内存管理 |
| L4 | 设备驱动抽象层 | WASM | USB类驱动、图形驱动、音频驱动、文件系统驱动 |
| L5 | 应用层 | WASM | 用户上传的.exe程序，运行在兼容层之上 |
| L6 | 用户界面层 | TypeScript/JS | Windows风格桌面环境、窗口管理器、任务栏、开始菜单 |

- [x] **里程碑M0**：六层架构全部定义完毕，层间接口契约明确，各层独立可测试


## 第二部分：L1 浏览器宿主层

> **职责**：提供浏览器原生能力，作为兼容层与物理世界的桥梁

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 1.1 | 基于Chromium ≥ 120版本 | 所有开发和测试在Chrome/Edge ≥ 120上通过 | [x] |
| 1.2 | HTTPS安全上下文强制 | 页面必须在localhost或HTTPS下运行，否则拒绝启动 | [x] |
| 1.3 | 使用OPFS（源私有文件系统） | 每个Windows应用/游戏拥有独立的、隔离的虚拟硬盘 | [x] |
| 1.4 | OPFS容量管理 | 单个虚拟硬盘容量可配置（默认2GB），支持扩容操作 | [x] |
| 1.5 | 使用Web Workers实现进程隔离 | 每个Windows进程运行在独立的Web Worker中 | [x] |
| 1.6 | 使用SharedArrayBuffer实现进程间通信 | 支持跨Worker的共享内存通信，需配置跨域隔离头 | [x] |
| 1.7 | 使用WebGPU作为图形后端 | 所有2D/3D渲染最终通过WebGPU提交给GPU执行 | [x] |
| 1.8 | 使用AudioWorklet处理音频 | 音频流在独立音频线程中混音和输出，延迟 ≤ 50ms | [ ] |
| 1.9 | 实现WebUSB设备枚举 | `navigator.usb.getDevices()` 可列出所有已授权USB设备 | [x] |
| 1.10 | 实现WebUSB配对请求 | `navigator.usb.requestDevice({ filters: [...]})` 可弹出设备选择器 | [x] |
| 1.11 | 实现USB插拔监听 | `navigator.usb.addEventListener('connect'/'disconnect')` 正常工作 | [x] |


## 第三部分：L2 系统调用桥接层

> **职责**：将WASM层的系统调用请求，转换成浏览器宿主层的API调用

### 3.1 文件系统桥接

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 3.1.1 | 实现CreateFile映射 | Windows的CreateFile → OPFS的FileSystemFileHandle.open() | [x] |
| 3.1.2 | 实现ReadFile映射 | 同步/异步读取，支持缓冲区大小动态调整 | [x] |
| 3.1.3 | 实现WriteFile映射 | 同步/异步写入，支持追加模式和覆盖模式 | [x] |
| 3.1.4 | 实现SetFilePointer映射 | 支持FILE_BEGIN、FILE_CURRENT、FILE_END三种偏移模式 | [x] |
| 3.1.5 | 实现FindFirstFile/FindNextFile映射 | 支持通配符（*和?）的目录遍历 | [x] |
| 3.1.6 | 实现CreateDirectory/RemoveDirectory映射 | 支持嵌套目录创建和递归删除 | [x] |
| 3.1.7 | 实现DeleteFile映射 | 文件删除，返回Windows兼容的错误码 | [x] |
| 3.1.8 | 实现GetFileAttributes/SetFileAttributes映射 | 支持只读、隐藏、系统、归档属性 | [x] |
| 3.1.9 | 实现文件锁（共享锁/排他锁） | 多进程并发访问同一文件时，锁行为与Windows一致 | [x] |

### 3.2 图形桥接

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 3.2.1 | 实现GDI 32位色深渲染 | 所有GDI绘图操作（BitBlt、StretchBlt、PatBlt等）在32位色深下正确渲染 | [ ] |
| 3.2.2 | 实现GDI文本渲染（GDI32!TextOut） | 支持TrueType字体，字体名称映射到Web可用字体 | [ ] |
| 3.2.3 | 实现GDI区域裁剪（GDI32!SelectClipRgn） | 支持矩形、椭圆、多边形裁剪区域 | [ ] |
| 3.2.4 | 实现窗口DC和内存DC的双缓冲 | 所有绘图先在内存DC完成，SwapBuffers时一次性提交到屏幕 | [ ] |
| 3.2.5 | 实现Direct3D 7/8/9固定管线转换 | 将D3D固定管线渲染命令转换为WebGPU渲染通道 | [ ] |
| 3.2.6 | Direct3D纹理上传优化 | 纹理数据从WASM内存到GPU纹理的上传延迟 ≤ 16ms | [ ] |
| 3.2.7 | 实现OpenGL 1.1-1.5兼容层 | 至少支持glBegin/glEnd、glVertex、glColor、glMatrix等核心函数 | [ ] |
| 3.2.8 | 实现窗口事件映射 | Windows的WM_PAINT、WM_SIZE、WM_MOUSEMOVE等映射到浏览器DOM事件 | [ ] |
| 3.2.9 | 实现光栅操作（ROP2/ROP3） | 支持全部16种ROP2和256种ROP3三元光栅操作 | [ ] |

### 3.3 音频桥接

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 3.3.1 | 实现waveOutOpen/waveOutWrite映射 | 音频PCM数据通过AudioWorklet输出，采样率 ≥ 44100Hz | [ ] |
| 3.3.2 | 实现DirectSound主缓冲区和辅助缓冲区 | 支持多个音频流同时播放，自动混音 | [ ] |
| 3.3.3 | 音频缓冲区低延迟 | 端到端音频延迟（从WriteFile到扬声器）≤ 100ms | [ ] |
| 3.3.4 | 支持音量控制 | 主音量和每个音频流的独立音量调节 | [ ] |

### 3.4 USB桥接（锦上添花）

> **策略**：不模拟完整的USB控制器，而是通过WASI-USB规范 + WebUSB API实现直通访问

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 3.4.1 | 实现WASI-USB Host适配器 | 实现wasi-usb规范中定义的所有Host接口，底层使用WebUSB API | [x] |
| 3.4.2 | 设备发现：wasi_usb_device_list | 返回当前所有已授权的USB设备列表（设备描述符、配置描述符） | [x] |
| 3.4.3 | 设备打开：wasi_usb_device_open | 通过设备路径或VID/PID打开设备，返回设备句柄 | [x] |
| 3.4.4 | 控制传输：wasi_usb_control_transfer | 支持bmRequestType、bRequest、wValue、wIndex、wLength完整参数 | [x] |
| 3.4.5 | 批量传输：wasi_usb_bulk_transfer | 支持IN/OUT端点的批量数据传输，超时可配置（默认5000ms） | [x] |
| 3.4.6 | 中断传输：wasi_usb_interrupt_transfer | 支持IN/OUT端点中断传输，支持超时中断 | [x] |
| 3.4.7 | 设备关闭：wasi_usb_device_close | 释放设备句柄，清理资源 | [x] |
| 3.4.8 | 权限管理 | 每次设备访问都需要用户通过浏览器弹窗授权；企业环境可配置组策略 | [ ] |
| 3.4.9 | 设备类驱动示例（HID） | 实现一个HID键盘/鼠标类驱动，通过WebUSB与真实HID设备通信 | [ ] |
| 3.4.10 | 设备类驱动示例（大容量存储） | 实现一个U盘类驱动，通过WebUSB读写U盘的扇区数据 | [ ] |


## 第四部分：L3 Windows兼容核心层

> **职责**：这是整个项目的"大脑"，负责运行x86机器码、拦截系统调用、管理进程

### 4.1 x86指令JIT翻译器

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 4.1.1 | 支持x86 32位保护模式指令集 | 覆盖i386指令集的主要指令（MOV、ADD、SUB、JMP、CALL、RET、PUSH、POP等） | [ ] |
| 4.1.2 | 支持FPU指令（x87） | 至少支持FADD、FSUB、FMUL、FDIV、FCOM、FILD、FSTP等常用浮点指令 | [ ] |
| 4.1.3 | 支持MMX指令集 | 至少支持MMX的算术运算指令 | [ ] |
| 4.1.4 | 支持SSE/SSE2指令集 | 至少支持SSE的浮点运算（ADDPS、MULPS等）和SSE2的整数运算 | [ ] |
| 4.1.5 | WASM JIT代码生成 | 每条x86指令翻译成等价的WASM指令序列，生成WASM函数体 | [x] |
| 4.1.6 | 基本块（Basic Block）翻译 | 以基本块为单位进行JIT编译，支持条件跳转的延迟编译 | [x] |
| 4.1.7 | 代码缓存 | 已编译的WASM代码缓存到内存中，相同代码段不重复编译，缓存命中率 ≥ 90% | [x] |
| 4.1.8 | 性能目标 | JIT编译后代码的执行速度，不低于原生C++模拟器的60%（SPECint基准测试） | [ ] |
| 4.1.9 | 内存保护 | 对WASM线性内存的读写操作进行边界检查，防止越界访问 | [ ] |

### 4.2 Windows API拦截器（Syscall Hooking）

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 4.2.1 | PE文件加载器 | 解析.exe/.dll的PE格式，加载代码段、数据段、资源段到WASM线性内存 | [x] |
| 4.2.2 | IAT表重写 | 扫描导入表（Import Address Table），把kernel32.dll、user32.dll、gdi32.dll的API地址替换为拦截函数的地址 | [x] |
| 4.2.3 | 系统DLL Stub生成 | 在WASM内存中生成假的kernel32.dll、user32.dll、gdi32.dll，每个导出函数的代码只有一条"陷阱"指令 | [x] 注：采用直接 IAT→trap stub 方案（`mov eax,idx; int 0x2E; ret`），不落盘假 DLL 文件 |
| 4.2.4 | API调用拦截 | 当Windows程序调用系统DLL中的函数时，触发陷阱，把控制权转移到TypeScript桥接层 | [x] |
| 4.2.5 | 参数编组（Marshalling） | 将Windows API的调用参数（指针、结构体、句柄）从WASM内存读取，转换成TypeScript可用的数据结构 | [x] |
| 4.2.6 | 返回值编组 | 将TypeScript层处理结果的返回值，写回WASM内存中Windows程序期望的位置 | [x] |
| 4.2.7 | 错误码转换 | 将桥接层产生的错误码，转换为Windows风格的GetLastError()错误码 | [x] |

### 4.3 进程与内存管理

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 4.3.1 | 进程创建（CreateProcess） | 支持加载.exe并启动新进程，分配独立的4GB虚拟地址空间 | [x] |
| 4.3.2 | 进程终止（ExitProcess/TerminateProcess） | 进程退出时清理所有资源（内存、句柄、线程） | [x] |
| 4.3.3 | 线程创建（CreateThread） | 支持创建新线程，分配独立的栈空间（默认1MB） | [x] |
| 4.3.4 | 线程调度 | 每个Windows线程对应一个Web Worker，通过Worker的消息机制实现调度 | [ ] |
| 4.3.5 | 虚拟内存分配（VirtualAlloc） | 支持MEM_COMMIT、MEM_RESERVE、MEM_RELEASE操作，页大小4KB | [x] |
| 4.3.6 | 虚拟内存保护（VirtualProtect） | 支持PAGE_READONLY、PAGE_READWRITE、PAGE_EXECUTE_READWRITE等保护属性 | [x] |
| 4.3.7 | 堆管理（HeapCreate/HeapAlloc/HeapFree） | 支持进程默认堆和自定义堆，堆块管理采用空闲链表算法 | [x] |
| 4.3.8 | 句柄表 | 每个进程维护一个全局句柄表，管理所有打开的内核对象（文件、设备、事件、互斥体等） | [x] |
| 4.3.9 | 互斥体（CreateMutex） | 支持命名和匿名互斥体，跨进程同步 | [x] |
| 4.3.10 | 事件（CreateEvent） | 支持手动重置和自动重置事件，跨进程同步 | [x] |
| 4.3.11 | 信号量（CreateSemaphore） | 支持计数信号量，最大计数值可配置 | [x] |

### 4.4 内核对象管理

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 4.4.1 | 内核对象引用计数 | 所有内核对象（文件、设备、同步对象）采用引用计数管理，引用计数归零时自动销毁 | [x] |
| 4.4.2 | 跨进程对象共享 | 支持通过DuplicateHandle跨进程共享内核对象句柄 | [ ] |
| 4.4.3 | 命名对象命名空间 | 支持命名的互斥体、事件、信号量，命名空间在系统范围内唯一 | [x] |


## 第五部分：L4 设备驱动抽象层

> **职责**：提供Windows设备驱动模型的WASM实现，承接PnP Manager和WDM/KS相关接口

### 5.1 USB驱动模型

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 5.1.1 | Windows USB驱动接口 | 实现Windows USB驱动模型所需的核心接口（IRP处理、URB构建与提交） | [x] |
| 5.1.2 | USB请求块（URB）构建 | 支持构建URB_FUNCTION_CONTROL_TRANSFER、URB_FUNCTION_BULK_OR_INTERRUPT_TRANSFER等URB类型 | [ ] |
| 5.1.3 | URB→WASI-USB映射 | 把Windows的URB请求，映射为WASI-USB的control_transfer、bulk_transfer等调用 | [ ] |
| 5.1.4 | PnP（即插即用）通知 | 当物理USB设备插拔时，通过PnP机制通知Windows驱动栈（设备添加/移除事件） | [x] |
| 5.1.5 | HID类驱动（复用Wine代码） | 将Wine的HID驱动（hidclass.sys、hidparse.sys）编译为WASM，通过USB驱动模型与物理HID设备通信 | [ ] |
| 5.1.6 | 大容量存储类驱动（复用Wine代码） | 将Wine的USB大容量存储驱动编译为WASM，支持FAT32文件系统读写 | [ ] |
| 5.1.7 | USB CDC（通信设备类）驱动示例 | 实现一个虚拟串口驱动，让Windows程序能通过USB与真实串口设备通信 | [ ] |

### 5.2 图形驱动

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 5.2.1 | 显示驱动接口（DXGKRNL） | 实现Windows显示驱动模型的miniport接口，与图形桥接层对接 | [ ] |
| 5.2.2 | 多显示器支持 | 支持至少2个虚拟显示器，每个可独立设置分辨率和色深 | [ ] |


## 第六部分：L6 用户界面层

> **职责**：提供Windows风格的桌面环境，让用户直观操作系统和应用

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 6.1 | Windows桌面环境渲染 | 基于HTML/CSS渲染包含壁纸、桌面图标的完整桌面 | [x] |
| 6.2 | 窗口管理器 | 支持多个应用窗口的创建、移动、缩放、关闭、Z序管理 | [x] |
| 6.3 | 窗口标题栏和边框 | 标题栏包含标题文字、最小化、最大化、关闭按钮；边框支持鼠标拖拽调整大小 | [x] |
| 6.4 | 任务栏 | 底部任务栏显示所有打开的窗口图标，支持点击切换 | [x] |
| 6.5 | 开始菜单 | 应用列表按分类排列，点击启动应用，含关机选项 | [x] |
| 6.6 | 拖拽上传EXE启动 | 用户从本地文件系统拖拽.exe文件到浏览器窗口，自动加载并启动该应用 | [x] |
| 6.7 | 应用图标提取 | 从PE资源中提取应用的图标（.ico），显示在桌面和标题栏上 | [x] |
| 6.8 | 右键上下文菜单 | 桌面上右键弹出包含"刷新"、"新建文件夹"等选项的上下文菜单 | [x] |
| 6.9 | 控制面板 | 提供系统信息、虚拟硬盘容量、显示设置等基本配置界面 | [x] |


## 第七部分：性能指标与验收标准

### 7.1 性能指标

| 序号 | 指标名称 | 目标值 | 测试方法 | 状态 |
|------|----------|--------|----------|------|
| 7.1.1 | 冷启动时间 | ≤ 3秒 | 从页面加载到桌面环境就绪 | [ ] |
| 7.1.2 | 应用启动时间 | ≤ 2秒 | 从双击应用到第一个窗口显示 | [ ] |
| 7.1.3 | 帧率（2D桌面） | ≥ 60 FPS | 桌面拖动窗口、滚动等操作 | [ ] |
| 7.1.4 | 帧率（3D游戏） | ≥ 30 FPS | 《Quake III》或同类3D游戏Demo | [ ] |
| 7.1.5 | 内存占用（空闲） | ≤ 200MB | 桌面环境+兼容核心的WASM内存 | [ ] |
| 7.1.6 | 内存占用（单应用） | ≤ 1GB | 运行一个Windows应用时的总内存 | [ ] |
| 7.1.7 | JIT编译吞吐量 | ≥ 10MB/秒 | 每秒翻译的x86机器码大小 | [ ] |
| 7.1.8 | 系统调用延迟（快速路径） | ≤ 0.5μs | GetTickCount等简单调用的往返延迟 | [ ] |
| 7.1.9 | 系统调用延迟（慢速路径） | ≤ 100μs | 文件读写等复杂调用的往返延迟 | [ ] |
| 7.1.10 | USB批量传输吞吐量 | ≥ 10MB/秒 | 从USB设备批量读取数据的速率 | [ ] |

### 7.2 兼容性验收

| 序号 | 测试应用 | 预期结果 | 状态 |
|------|----------|----------|------|
| 7.2.1 | Windows 记事本 (notepad.exe) | 正常启动，可编辑保存文件 | [ ] |
| 7.2.2 | Windows 扫雷 (winmine.exe) | 正常启动，游戏可玩，界面完整 | [ ] |
| 7.2.3 | Windows 纸牌 (sol.exe) | 正常启动，游戏可玩，界面完整 | [ ] |
| 7.2.4 | Windows 画图 (mspaint.exe) | 正常启动，绘图功能可用 | [ ] |
| 7.2.5 | 命令行应用 (cmd.exe + dir) | 正常启动，命令行交互可用 | [ ] |
| 7.2.6 | Visual C++ 运行时 (CRT) 测试程序 | 所有CRT函数（malloc、fopen、printf等）正常工作 | [ ] |
| 7.2.7 | Windows 3D 管道屏保 | 3D渲染正常，帧率≥30FPS | [ ] |
| 7.2.8 | Quake III Arena Demo | 3D游戏可玩，帧率≥30FPS | [ ] |
| 7.2.9 | Arduino IDE（Windows版） | 正常启动，可通过WebUSB烧录程序到真实Arduino板 | [ ] |


## 第八部分：安全与隔离

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 8.1 | 浏览器沙箱隔离 | 所有WASM代码运行在浏览器沙箱中，无法逃逸到宿主机 | [ ] |
| 8.2 | 进程间隔离 | 每个Windows进程运行在独立的Web Worker中，一个进程崩溃不影响其他进程 | [x] |
| 8.3 | 内存安全 | WASM线性内存的边界检查由WASM运行时保证，无法越界访问宿主机内存 | [ ] |
| 8.4 | USB访问授权 | 每次USB设备访问都必须经过用户明确授权（浏览器弹窗） | [x] |
| 8.5 | 文件系统隔离 | 每个应用的虚拟硬盘相互隔离，无法访问其他应用的文件 | [ ] |
| 8.6 | 网络隔离（可选） | 支持配置网络访问策略，阻止或允许Windows应用的网络请求 | [ ] |
| 8.7 | XSS防护 | 桌面环境的所有用户输入经过严格过滤，防止XSS注入 | [ ] |


## 第九部分：工程化要求

| 序号 | 技术要求 | 验收标准 | 状态 |
|------|----------|----------|------|
| 9.1 | 项目构建工具 | 使用Vite或Webpack 5作为构建工具，支持热更新开发 | [x] |
| 9.2 | WASM编译工具链 | 使用Emscripten 3.1.x 或 wasi-sdk 编译C++/Rust代码为WASM | [ ] |
| 9.3 | 单元测试覆盖率 | 核心模块（JIT、API拦截器、文件系统桥接）单元测试覆盖率 ≥ 80% | [ ] |
| 9.4 | 集成测试 | 每个兼容性测试应用（7.2节）有对应的自动化集成测试 | [ ] |
| 9.5 | 性能基准测试 | 建立性能回归基准，每次PR自动运行基准测试，性能下降超过5%即阻断合并 | [ ] |
| 9.6 | GitHub Actions CI/CD | 每次push自动运行测试、构建、生成可部署的静态站点 | [x] |
| 9.7 | 在线Demo部署 | 通过GitHub Pages或Cloudflare Pages部署可用的在线Demo | [x] |
| 9.8 | 项目文档 | README包含架构图、快速开始、开发指南；API文档自动生成 | [x] |
| 9.9 | 贡献指南 | CONTRIBUTING.md包含开发环境配置、代码规范、PR流程 | [x] |


## 第十部分：里程碑规划

| 阶段 | 目标 | 核心交付物 | 预计时间 | 状态 |
|------|------|------------|----------|------|
| P0 | 基础设施搭建 | 构建工具、WASM编译链、CI/CD、在线Demo部署 | 2周 | [ ] |
| P1 | L3核心：PE加载 + JIT翻译 | 能加载并运行最简单的"Hello World"控制台程序 | 2个月 | [ ] |
| P2 | L2文件系统桥接 | 控制台程序能读写虚拟硬盘中的文件 | 1个月 | [ ] |
| P3 | L2图形桥接 + L6桌面 | 显示Windows风格的窗口，运行notepad.exe | 2个月 | [ ] |
| P4 | 音频桥接 + 进程管理 | 运行带音频的应用（扫雷、纸牌） | 1个月 | [ ] |
| P5 | 3D图形（Direct3D→WebGPU） | 运行3D游戏（Quake III） | 2个月 | [ ] |
| P6 | USB支持（锦上添花） | 运行Arduino IDE并烧录到真实USB设备 | 1.5个月 | [ ] |
| P7 | 性能优化 + 兼容性扩展 | 达到7.1节所有性能指标，通过7.2节所有兼容性测试 | 2个月 | [ ] |
| **总计** | | | **约12-14个月** | |


## 文档版本

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-08-18 | 初始版本，包含完整技术设计 | - |


> **最终验收标准**：当上述文档中所有 `[ ]` 标记都被打上 `[x]` 时，项目即宣告成功。届时，这将是一个**行业内无人做到**的里程碑——在浏览器中通过WASM + HLE技术原生运行Windows应用，并可选支持物理USB设备访问。