import { jsx } from "react/jsx-runtime";
import { NotepadApp } from "./apps/NotepadApp";
import { SystemInfoApp } from "./apps/SystemInfoApp";
import { MinesweeperApp } from "./apps/MinesweeperApp";
import { FileExplorerApp } from "./apps/FileExplorerApp";
import { InstallerApp } from "./apps/InstallerApp";
import { ImageViewerApp } from "./apps/ImageViewerApp";
import { RunExecutableApp } from "./apps/RunExecutableApp";
const DEFAULT_APPS = [
  {
    appId: "notepad",
    name: "Notepad",
    icon: "\u{1F4DD}",
    description: "Simple text editor",
    group: "Accessories",
    render: (args) => /* @__PURE__ */ jsx(NotepadApp, { initialFile: args?.path })
  },
  {
    appId: "minesweeper",
    name: "Minesweeper",
    icon: "\u{1F4A3}",
    description: "Classic minesweeper",
    group: "Games",
    render: () => /* @__PURE__ */ jsx(MinesweeperApp, {})
  },
  {
    appId: "system-info",
    name: "System Information",
    icon: "\u{1F5A5}",
    description: "Kernel, processes and disk status",
    group: "System",
    render: () => /* @__PURE__ */ jsx(SystemInfoApp, {})
  },
  {
    appId: "file-explorer",
    name: "File Explorer",
    icon: "\u{1F4C2}",
    description: "Browse the virtual disk",
    group: "System",
    render: (args) => /* @__PURE__ */ jsx(FileExplorerApp, { initialPath: args?.path })
  },
  {
    appId: "command-prompt",
    name: "Command Prompt",
    icon: "\u{1F5A5}",
    description: "Run the bundled Windows cmd.exe (real x86 PE) over the virtual disk",
    group: "System",
    // Launch is special-cased in DesktopController: it runs the bundled
    // cmd.exe as a real guest process with a console terminal (stdin/stdout),
    // replacing the old JS interpreter shell.
    render: () => null
  },
  {
    appId: "installer",
    name: "Installer",
    icon: "\u{1F4E6}",
    description: "Install applications (.bkapp) onto the virtual disk",
    group: "System",
    render: (args) => /* @__PURE__ */ jsx(InstallerApp, { initialPackagePath: args?.path })
  },
  {
    appId: "image-viewer",
    name: "Photos",
    icon: "\u{1F5BC}\uFE0F",
    description: "View images from the virtual disk",
    group: "Accessories",
    render: (args) => /* @__PURE__ */ jsx(ImageViewerApp, { initialFile: args?.path })
  },
  {
    appId: "exe-runner",
    name: "Run Executable",
    icon: "\u2699\uFE0F",
    description: "Run a Windows executable from the virtual disk",
    group: "System",
    render: (args) => /* @__PURE__ */ jsx(RunExecutableApp, { initialFile: args?.path })
  },
  {
    appId: "windows-notepad",
    name: "Notepad (Windows)",
    icon: "\u{1F4DD}",
    description: "Run the bundled Windows notepad.exe with its MUI resources",
    group: "System",
    // Launch is special-cased in DesktopController: it runs the bundled
    // notepad as a real guest window (no application-shell window).
    render: () => null
  }
];
function toDesktopApp(app) {
  return {
    appId: app.appId,
    name: app.name,
    icon: app.icon,
    description: app.description,
    group: app.group,
    launch: () => Promise.resolve()
  };
}
export {
  DEFAULT_APPS,
  toDesktopApp
};
